@page
@model SOA_CA2.Pages.PlantPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plant[0].plant_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plant[0].plant_type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plant[0].plant_price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Plant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.plant_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.plant_type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.plant_price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.plant_id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.plant_id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.plant_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
